import React from 'react';

const  Copyfile = () =>{
    return (
        <div>
            <input type="text" value={text} onChange={}/>
            <button>Add</button>
        </div>
    );
}

export default Copyfile;